{
  "name": "amberbase-monorepo",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "docs": "pnpm -r run docs",
    "test": "pnpm -r --if-present run test && pnpm exec vitest run --config vitest.config.base.ts --dir src/shared",
    "lint": "oxlint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "oxlint": "^1.76.0",
    "prettier": "^3.9.6",
    "vitest": "^4.1.10"
  }
}
